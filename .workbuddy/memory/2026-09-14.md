# 2026-09-14

- 改造 `api/_lib/roundtableHandler.js` 的 `buildRolePrompt`：四个圆桌角色（advocate/critic/researcher/moderator）的 prompt 从"格式约束"改为"性格 + 辩论策略"约束，让发言更像人在辩论。
  - 保留偏差可见机制与全部硬约束（研究专家不答应然问题+每轮指出证据答不了的问题；主持人禁用和稀泥修辞+每轮前提追问+不推进方案；全员 80-150 字）。
  - 格式标签（【事实主张】等、证据强度标签开头、主持人固定句式）全部降级为关键处使用。
  - 只改 buildRolePrompt；ROLES、handleRoundtable 返回结构、llmClient.js、chat.js、前端调度均未动。语法校验通过（node --check）。

- 第二轮：修复三个结构性缺口（选择了路径A，纯 prompt，未改调度与协议）。
  - moderator case 新增【深挖前提】【引入缺席者】（约每两轮一次+说明论证缺口）【承接事实校准】三项职责；超长取舍优先级：深挖前提 > 引入缺席者 > 承接事实校准。
  - researcher case 同步微调：标注证据边界时必须点名是哪一方哪个主张受影响，保证可被主持人承接。
  - 全部旧硬约束保留；handleRoundtable、返回结构、请求协议、前端均未动。路径B（triggeredBy 触发机制）未实现，作为后续可选。
