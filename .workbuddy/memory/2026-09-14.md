# 2026-09-14

- 改造 `api/_lib/roundtableHandler.js` 的 `buildRolePrompt`：四个圆桌角色（advocate/critic/researcher/moderator）的 prompt 从"格式约束"改为"性格 + 辩论策略"约束，让发言更像人在辩论。
  - 保留偏差可见机制与全部硬约束（研究专家不答应然问题+每轮指出证据答不了的问题；主持人禁用和稀泥修辞+每轮前提追问+不推进方案；全员 80-150 字）。
  - 格式标签（【事实主张】等、证据强度标签开头、主持人固定句式）全部降级为关键处使用。
  - 只改 buildRolePrompt；ROLES、handleRoundtable 返回结构、llmClient.js、chat.js、前端调度均未动。语法校验通过（node --check）。

- 第二轮：修复三个结构性缺口（选择了路径A，纯 prompt，未改调度与协议）。
  - moderator case 新增【深挖前提】【引入缺席者】（约每两轮一次+说明论证缺口）【承接事实校准】三项职责；超长取舍优先级：深挖前提 > 引入缺席者 > 承接事实校准。
  - researcher case 同步微调：标注证据边界时必须点名是哪一方哪个主张受影响，保证可被主持人承接。
  - 全部旧硬约束保留；handleRoundtable、返回结构、请求协议、前端均未动。路径B（triggeredBy 触发机制）未实现，作为后续可选。

- 第三轮：critic（批判者）换模型为千帆 GLM-5.3（接入点 ID `glm-5.3`），其他一切不动。
  - 关键结论：`api/_lib/llmClient.js` 的模型名**已经是参数化的**（第 25 行解构默认值 `model = DEFAULT_MODEL`，DEFAULT_MODEL='deepseek-v3.2'，BASE_URL 为千帆 /v2），因此 llmClient.js **无需改动**。
  - roundtableHandler.js 两处改动：① ROLES 里仅 critic 增加 `model: 'glm-5.3'`；② callLLM 调用处增加 `model: roleDef.model || undefined`（undefined 会触发默认参数，向后兼容）。
  - 未动 critic 的 system prompt、其他三角色 prompt/模型、返回结构、chat.js、前端。语法校验通过。

- 第四轮：critic 的请求超时放宽（GLM-5.3 是推理模型，首字延迟长，8 秒超时导致「LLM 请求超时」）。
  - roundtableHandler.js 两处改动：ROLES 里 critic 增加 `timeout: 30000`；callLLM 处 `timeout: 8000` 改为 `timeout: roleDef.timeout || 8000`（其他角色无该字段 -> 回落 8 秒，向后兼容）。
  - **重要待办风险**：`vercel.json` 未配置 `maxDuration`，Vercel 函数默认执行上限可能低于 30 秒（Hobby 默认 10s 级），若 critic 出现 504/函数被平台掐断而非应用层「LLM 请求超时」，需要在 vercel.json 的 functions 里加 maxDuration（如 60）。升级路径：timeout 30000 -> 60000，或 critic 换 glm-5.3-flash（更快但推理略弱）。

- 第五轮：critic 模型 glm-5.3 → glm-5.2，并支持禁用思考（GLM-5.3 强制思考，把推理草稿吐进正文）。
  - `llmClient.js`：**原本不支持透传额外参数**（请求体是写死的 4 个字段），新增可选 `disableThinking = false`，为 true 时用展开语法拼 `thinking: { type: 'disabled' }`。
  - `roundtableHandler.js`：ROLES 的 critic 改为 `model: 'glm-5.2', timeout: 30000, disableThinking: true`；callLLM 处新增 `disableThinking: roleDef.disableThinking || false`。
  - 已用 stub fetch 实测请求体：critic 带 thinking 字段，默认角色不带，向后兼容确认。
  - 查阅结论：千帆 API 参考（创建模型响应）确有 `thinking.type` = enabled/disabled；智谱官方明确 GLM-5.3/5.3-FLASH 强制思考不可关、GLM-5.2 可用 `thinking:{type:'disabled'}` 关闭。千帆 /v2/chat/completions 形态未逐字确认，若返回 400 未知字段，回退方案为 `reasoning_effort: 'none'`（glm-5.2 支持）或去掉该字段。



